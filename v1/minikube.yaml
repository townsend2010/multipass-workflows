description: minikube is local Kubernetes

instances:
  minikube:
    image: "lts"
    limits:              # optional
      min-cpu: 2
      min-mem: 2G
      min-disk: 20G
    cloud-init:
      runcmd:
      - echo "We did it!" > /tmp/wow
      - echo "This works too!" > /tmp/too
